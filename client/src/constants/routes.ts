export enum RoutesPath {
    LOGIN = '/signIn',
    REGISTER = '/signUp',
    HOME = '/',
    SEARCH = '/search',
}

export enum AuthApiRoutes {
    LOGIN = '/auth/login',
    REGISTER = '/auth/register',
    AUTH_ME = '/auth/me',
}
